"""Local configs override for Being-H when running from `Being-H_xlerobot/`.

We intentionally provide a `configs.*` namespace here (same import path as Being-H)
so we can add XLerobot datasets/configs without modifying upstream code.
"""

